// run with < input.txt in STDIN
// edit WITH_AIM accordingly

#include <stdio.h>
#include <string.h>

#define GRID_HEIGHT 5
#define FIRST_LINE_LENGTH 290

typedef struct {
        int** content;
        int* marked;
} Board;


int part1() {
        char first_line[FIRST_LINE_LENGTH];
        scanf("%s", first_line);
}

